# PostgreSQL. Versions 9.3 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem "pg"
#
default: &default
  adapter: postgresql
  encoding: unicode
  username: rog
  password: '2654'
  port: 5432
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: vegan4days_rails_development
  username: rog
  password: '2654'
test:
  <<: *default
  database: vegan4days_rails_test
production:
  <<: *default
  database: vegan4days_rails_production
  username: vegan4days_rails
  password: <%= ENV["VEGAN4DAYS_RAILS_DATABASE_PASSWORD"] %>
